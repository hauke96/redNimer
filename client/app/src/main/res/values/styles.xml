<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AddButtonStyle" parent="Base.Widget.AppCompat.Button.Borderless">
        <item name="android:background">@color/lightGray</item>
    </style>

    <style name="SwitchStyle">
        <item name="colorControlActivated">@color/mediumGray</item>
        <item name="colorSwitchThumbNormal">@color/mediumGray</item>
        <item name="android:colorForeground">@color/mediumGray</item>
    </style>

</resources>
